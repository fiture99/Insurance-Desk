kind = "web"
previewPath = "/"
title = "AfricMed Insurance Desk"
version = "1.0.0"
id = "artifacts/insurance-desk"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23165

[services.development]
run = "pnpm --filter @workspace/insurance-desk run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/insurance-desk", "run", "build" ]
publicDir = "artifacts/insurance-desk/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23165"
BASE_PATH = "/"
